# YouTube Transcripts

Transcripts of recent videos from the experts who publish on YouTube, organized one
folder per author. Transcripts are pulled programmatically via `yt-dlp` (to list each
channel's recent uploads) + `youtube-transcript-api` (to fetch the caption track).

Each transcript file includes the video title, URL, publish date, and the full text.
